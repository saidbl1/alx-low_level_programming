#include <stdio.h>

/**
 * main - program that prints an exact string followed by a new line
 *
 * Return: Always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
